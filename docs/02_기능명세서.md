# 📋 기능 명세서 - Project Tracker

## 문서 정보
- **프로젝트명**: Project Tracker
- **버전**: 1.0
- **작성일**: 2025년 11월 23일

---

## 1. 기능 목록

| ID | 기능명 | 우선순위 | 예상 시간 | 상태 |
|----|--------|----------|-----------|------|
| F-001 | 프로젝트 관리 | HIGH | 4h | 대기 |
| F-002 | Kanban 보드 | HIGH | 8h | 대기 |
| F-003 | 태스크 관리 | HIGH | 6h | 대기 |
| F-004 | 진행률 대시보드 | HIGH | 6h | 대기 |
| F-005 | 마일스톤 관리 | MEDIUM | 3h | 대기 |
| F-006 | 회고 기록 (KPT) | MEDIUM | 4h | 대기 |
| F-007 | 통계 및 차트 | MEDIUM | 4h | 대기 |

**총 예상 시간**: 35시간 (7일 × 5시간/일)

---

## 2. F-001: 프로젝트 관리

### 2.1 프로젝트 생성

**기능**: 새로운 프로젝트 생성

**입력**:
- 프로젝트명 (필수, 200자 이내)
- 설명 (선택, TEXT)
- 시작일 (기본: 오늘)
- 목표 완료일 (선택)
- 상태 (기본: 진행중)

**처리 로직**:
```python
def create_project(name, description, start_date, target_end_date):
    """
    프로젝트 생성
    
    Returns:
        int: 생성된 프로젝트 ID
    """
    project = {
        'name': name,
        'description': description,
        'start_date': start_date or datetime.today(),
        'target_end_date': target_end_date,
        'status': 'active',
        'created_at': datetime.now()
    }
    
    project_id = db.insert_project(project)
    return project_id
```

**출력**:
- 성공: 프로젝트 ID 반환, 성공 메시지
- 실패: 에러 메시지 표시

**UI**:
```
사이드바에 "➕ 새 프로젝트" 버튼
클릭 시 모달/확장 패널 열림

[프로젝트 생성]
━━━━━━━━━━━━━━━━
프로젝트명*: [_______________________]
설명:        [_______________________]
             [_______________________]
시작일:      [2024-11-23 📅]
목표일:      [2024-12-31 📅]

[취소]  [생성]
```

**검증 규칙**:
- 프로젝트명 필수
- 프로젝트명 1~200자
- 시작일 ≤ 목표일 (목표일이 있는 경우)

### 2.2 프로젝트 목록 조회

**기능**: 모든 프로젝트 목록 표시

**처리 로직**:
```python
def get_projects(status=None):
    """
    프로젝트 목록 조회
    
    Args:
        status: 'active', 'completed', 'on_hold' 또는 None(전체)
    
    Returns:
        list: 프로젝트 리스트
    """
    query = "SELECT * FROM projects"
    if status:
        query += f" WHERE status = '{status}'"
    query += " ORDER BY created_at DESC"
    
    return db.execute_query(query)
```

**출력**:
- 프로젝트 카드 리스트
- 각 카드: 이름, 진행률, 상태, 마지막 업데이트

**UI**:
```
사이드바 - 프로젝트 목록

📊 대시보드
━━━━━━━━━━━━━━━━
📋 프로젝트

▶ 감정 일기 앱        53% ●
  최종 업데이트: 2시간 전
  
▶ 포트폴리오 웹사이트   20% ●
  최종 업데이트: 1일 전

+ 새 프로젝트
```

### 2.3 프로젝트 선택

**기능**: 작업할 프로젝트 선택

**처리 로직**:
```python
# Session State 사용
if 'current_project_id' not in st.session_state:
    st.session_state.current_project_id = None

def select_project(project_id):
    st.session_state.current_project_id = project_id
    st.rerun()
```

**UI**:
- 사이드바에서 클릭으로 선택
- 선택된 프로젝트 하이라이트
- 메인 영역 자동 업데이트

### 2.4 프로젝트 수정

**기능**: 프로젝트 정보 수정

**입력**: 프로젝트 생성과 동일

**UI**:
```
프로젝트 카드의 [...] 메뉴
→ "수정" 클릭
→ 수정 폼 표시 (생성 폼과 유사)
```

### 2.5 프로젝트 삭제

**기능**: 프로젝트 삭제 (연관 데이터 CASCADE)

**확인 다이얼로그**:
```
⚠️ 정말 삭제하시겠습니까?

프로젝트: 감정 일기 앱
연관 데이터:
- 태스크 15개
- 마일스톤 3개
- 회고 1개

모든 데이터가 영구 삭제됩니다.

[취소]  [삭제]
```

---

## 3. F-002: Kanban 보드

### 3.1 보드 레이아웃

**3개 컬럼**:
1. **To Do** (할 일)
2. **In Progress** (진행 중)
3. **Done** (완료)

**UI 구조**:
```python
col1, col2, col3 = st.columns(3)

with col1:
    st.markdown("### 📝 To Do")
    render_task_cards(status='todo')

with col2:
    st.markdown("### 🔄 In Progress")
    render_task_cards(status='in_progress')

with col3:
    st.markdown("### ✅ Done")
    render_task_cards(status='done')
```

### 3.2 태스크 카드 렌더링

**카드 구조**:
```python
def render_task_card(task):
    """
    태스크 카드 렌더링
    
    Args:
        task (dict): 태스크 정보
    """
    with st.container():
        # 제목
        st.markdown(f"**{task['title']}**")
        
        # 메타 정보
        col_a, col_b = st.columns(2)
        with col_a:
            # 태그
            st.caption(f"{get_tag_icon(task['tags'])}")
        with col_b:
            # 우선순위
            st.caption(f"{get_priority_badge(task['priority'])}")
        
        # 마감일
        if task['due_date']:
            days_left = (task['due_date'] - datetime.today()).days
            st.caption(f"📅 D-{days_left}")
        
        # 액션 버튼
        col1, col2, col3 = st.columns(3)
        with col1:
            if st.button("👁️", key=f"view_{task['id']}"):
                show_task_detail(task['id'])
        with col2:
            if st.button("✏️", key=f"edit_{task['id']}"):
                edit_task(task['id'])
        with col3:
            if st.button("🗑️", key=f"delete_{task['id']}"):
                delete_task(task['id'])
        
        st.divider()
```

### 3.3 상태 변경

**방법 1: 버튼 클릭**
```python
# 각 카드에 상태 변경 버튼
if task['status'] == 'todo':
    if st.button("→ 진행 중으로", key=f"start_{task['id']}"):
        update_task_status(task['id'], 'in_progress')

elif task['status'] == 'in_progress':
    col_left, col_right = st.columns(2)
    with col_left:
        if st.button("← 할 일로", key=f"back_{task['id']}"):
            update_task_status(task['id'], 'todo')
    with col_right:
        if st.button("완료 →", key=f"done_{task['id']}"):
            update_task_status(task['id'], 'done')
```

**방법 2: 드래그 앤 드롭 (Advanced)**
- Streamlit 기본 기능 없음
- 대안: selectbox로 상태 선택

**처리 로직**:
```python
def update_task_status(task_id, new_status):
    """
    태스크 상태 변경 및 시간 기록
    """
    timestamp = datetime.now()
    
    updates = {'status': new_status}
    
    if new_status == 'in_progress':
        updates['started_at'] = timestamp
    elif new_status == 'done':
        updates['completed_at'] = timestamp
    
    db.update_task(task_id, updates)
    st.success(f"상태가 '{new_status}'로 변경되었습니다")
    st.rerun()
```

### 3.4 태스크 빠른 추가

**UI**:
```
[Kanban 보드 상단]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
프로젝트: 감정 일기 앱 ▼

빠른 추가: [태스크 제목 입력...] [추가]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────┐ ┌──────────┐ ┌──────────┐
│  To Do   │ │ Progress │ │   Done   │
```

**처리**:
- 입력 후 Enter 또는 버튼 클릭
- 기본값: To Do, Medium 우선순위
- 상세 설정은 나중에 수정 가능

---

## 4. F-003: 태스크 관리

### 4.1 태스크 생성

**입력 필드**:
```python
with st.form("create_task"):
    title = st.text_input("제목*", max_chars=200)
    description = st.text_area("설명", height=100)
    
    col1, col2 = st.columns(2)
    with col1:
        status = st.selectbox("상태", 
            ["To Do", "In Progress", "Done"])
        priority = st.selectbox("우선순위",
            ["Low", "Medium", "High"])
    with col2:
        tags = st.multiselect("태그",
            ["Dev", "Design", "Test", "Deploy", "Docs"])
        due_date = st.date_input("마감일")
    
    estimated_hours = st.number_input("예상 시간 (시간)", 
        min_value=0.5, step=0.5)
    
    # 체크리스트
    st.markdown("**체크리스트** (선택)")
    checklist_items = st.text_area(
        "한 줄에 하나씩 입력",
        placeholder="API 키 발급\n연동 테스트\n에러 처리"
    )
    
    submitted = st.form_submit_button("생성")
    
    if submitted:
        if not title:
            st.error("제목은 필수입니다")
        else:
            create_task(...)
            st.success("태스크가 생성되었습니다!")
```

**처리 로직**:
```python
def create_task(project_id, title, description, status, 
                priority, tags, due_date, estimated_hours, 
                checklist_items):
    """
    태스크 생성
    """
    task = {
        'project_id': project_id,
        'title': title,
        'description': description,
        'status': status.lower().replace(' ', '_'),
        'priority': priority.lower(),
        'tags': ','.join(tags) if tags else None,
        'estimated_hours': estimated_hours,
        'due_date': due_date,
        'created_at': datetime.now()
    }
    
    task_id = db.insert_task(task)
    
    # 체크리스트 항목 저장
    if checklist_items:
        items = checklist_items.strip().split('\n')
        for item in items:
            if item.strip():
                db.insert_checklist_item(task_id, item.strip())
    
    return task_id
```

### 4.2 태스크 상세 보기

**모달/사이드바 형식**:
```python
@st.dialog("태스크 상세")
def show_task_detail(task_id):
    task = db.get_task(task_id)
    
    st.markdown(f"## {task['title']}")
    
    # 상태 배지
    st.markdown(f"**상태**: {get_status_badge(task['status'])}")
    st.markdown(f"**우선순위**: {get_priority_badge(task['priority'])}")
    
    # 설명
    if task['description']:
        st.markdown("### 설명")
        st.write(task['description'])
    
    # 체크리스트
    checklist = db.get_checklist_items(task_id)
    if checklist:
        st.markdown("### 체크리스트")
        for item in checklist:
            checked = st.checkbox(
                item['content'],
                value=item['is_checked'],
                key=f"check_{item['id']}"
            )
            if checked != item['is_checked']:
                db.update_checklist_item(item['id'], checked)
    
    # 메타 정보
    st.markdown("---")
    st.caption(f"📅 생성: {task['created_at']}")
    if task['started_at']:
        st.caption(f"▶️ 시작: {task['started_at']}")
    if task['completed_at']:
        st.caption(f"✅ 완료: {task['completed_at']}")
    
    # 액션 버튼
    col1, col2 = st.columns(2)
    with col1:
        if st.button("수정", use_container_width=True):
            edit_task(task_id)
    with col2:
        if st.button("삭제", use_container_width=True):
            delete_task(task_id)
```

### 4.3 태스크 수정

**UI**: 생성과 동일한 폼, 기존 값 미리 채움

**처리**:
```python
def edit_task(task_id):
    task = db.get_task(task_id)
    
    with st.form(f"edit_task_{task_id}"):
        # 기존 값으로 초기화된 입력 필드들
        title = st.text_input("제목*", value=task['title'])
        # ...
        
        submitted = st.form_submit_button("저장")
        if submitted:
            db.update_task(task_id, {...})
            st.success("수정되었습니다!")
            st.rerun()
```

### 4.4 태스크 삭제

**확인 후 삭제**:
```python
def delete_task(task_id):
    task = db.get_task(task_id)
    
    confirmed = st.warning(
        f"'{task['title']}' 태스크를 삭제하시겠습니까?"
    )
    
    col1, col2 = st.columns(2)
    with col1:
        if st.button("취소"):
            st.rerun()
    with col2:
        if st.button("삭제", type="primary"):
            db.delete_task(task_id)
            st.success("삭제되었습니다")
            st.rerun()
```

---

## 5. F-004: 진행률 대시보드

### 5.1 메트릭 카드

**계산 로직**:
```python
def calculate_metrics(project_id):
    """
    프로젝트 메트릭 계산
    """
    tasks = db.get_tasks(project_id)
    
    total_count = len(tasks)
    done_count = len([t for t in tasks if t['status'] == 'done'])
    in_progress_count = len([t for t in tasks if t['status'] == 'in_progress'])
    
    progress_rate = (done_count / total_count * 100) if total_count > 0 else 0
    
    # 남은 일 계산
    target_date = db.get_project(project_id)['target_end_date']
    days_left = (target_date - datetime.today()).days if target_date else None
    
    return {
        'total': total_count,
        'done': done_count,
        'in_progress': in_progress_count,
        'progress_rate': progress_rate,
        'days_left': days_left
    }
```

**UI 렌더링**:
```python
metrics = calculate_metrics(project_id)

col1, col2, col3, col4 = st.columns(4)

with col1:
    st.metric("전체 태스크", metrics['total'])

with col2:
    st.metric("완료", metrics['done'])

with col3:
    st.metric("진행률", f"{metrics['progress_rate']:.0f}%")

with col4:
    if metrics['days_left'] is not None:
        st.metric("남은 일", f"{metrics['days_left']}일")
    else:
        st.metric("남은 일", "미설정")
```

### 5.2 진행률 추이 차트

**데이터 수집**:
```python
def get_progress_history(project_id):
    """
    날짜별 완료율 계산
    """
    # completed_at 기준으로 날짜별 완료 개수 집계
    query = """
        SELECT DATE(completed_at) as date, COUNT(*) as count
        FROM tasks
        WHERE project_id = %s AND status = 'done'
        GROUP BY DATE(completed_at)
        ORDER BY date
    """
    
    history = db.execute_query(query, (project_id,))
    
    # 누적 합계 계산
    total_tasks = db.count_tasks(project_id)
    cumulative = []
    cumsum = 0
    
    for record in history:
        cumsum += record['count']
        cumulative.append({
            'date': record['date'],
            'progress': (cumsum / total_tasks * 100)
        })
    
    return pd.DataFrame(cumulative)
```

**Plotly 차트**:
```python
import plotly.express as px

df = get_progress_history(project_id)

fig = px.line(
    df, 
    x='date', 
    y='progress',
    title='진행률 추이',
    labels={'date': '날짜', 'progress': '완료율 (%)'}
)

fig.update_layout(
    yaxis_range=[0, 100],
    showlegend=False
)

# 목표선 추가
target_date = project['target_end_date']
if target_date:
    fig.add_hline(
        y=100, 
        line_dash="dash", 
        line_color="red",
        annotation_text="목표"
    )

st.plotly_chart(fig, use_container_width=True)
```

### 5.3 상태별 분포 차트 (원형)

**데이터**:
```python
def get_status_distribution(project_id):
    tasks = db.get_tasks(project_id)
    
    distribution = {
        'To Do': len([t for t in tasks if t['status'] == 'todo']),
        'In Progress': len([t for t in tasks if t['status'] == 'in_progress']),
        'Done': len([t for t in tasks if t['status'] == 'done'])
    }
    
    return distribution
```

**차트**:
```python
dist = get_status_distribution(project_id)

fig = px.pie(
    names=list(dist.keys()),
    values=list(dist.values()),
    title='상태별 태스크 분포',
    color_discrete_sequence=['#FFA07A', '#87CEEB', '#90EE90']
)

st.plotly_chart(fig, use_container_width=True)
```

### 5.4 태그별 분포 (막대 차트)

**데이터**:
```python
def get_tag_distribution(project_id):
    tasks = db.get_tasks(project_id)
    
    tag_count = {}
    for task in tasks:
        if task['tags']:
            tags = task['tags'].split(',')
            for tag in tags:
                tag = tag.strip()
                tag_count[tag] = tag_count.get(tag, 0) + 1
    
    return tag_count
```

**차트**:
```python
tag_dist = get_tag_distribution(project_id)

if tag_dist:
    df = pd.DataFrame([
        {'태그': k, '개수': v} 
        for k, v in tag_dist.items()
    ])
    
    fig = px.bar(
        df,
        x='태그',
        y='개수',
        title='태그별 태스크 분포'
    )
    
    st.plotly_chart(fig, use_container_width=True)
```

---

## 6. F-005: 마일스톤 관리

### 6.1 마일스톤 생성

**입력**:
```python
with st.form("create_milestone"):
    title = st.text_input("마일스톤명*")
    description = st.text_area("설명")
    target_date = st.date_input("목표 날짜*")
    
    submitted = st.form_submit_button("추가")
    
    if submitted:
        if not title or not target_date:
            st.error("필수 항목을 입력해주세요")
        else:
            db.insert_milestone(project_id, title, description, target_date)
            st.success("마일스톤이 추가되었습니다!")
```

### 6.2 마일스톤 목록 표시

**UI**:
```python
milestones = db.get_milestones(project_id)

st.markdown("### 📅 마일스톤")

for milestone in milestones:
    col1, col2, col3 = st.columns([0.1, 0.7, 0.2])
    
    with col1:
        # 체크박스
        is_completed = st.checkbox(
            "",
            value=milestone['is_completed'],
            key=f"milestone_{milestone['id']}"
        )
        if is_completed != milestone['is_completed']:
            db.update_milestone_status(milestone['id'], is_completed)
            st.rerun()
    
    with col2:
        # 제목 및 날짜
        status_icon = "✅" if milestone['is_completed'] else "⏳"
        st.markdown(f"**{milestone['title']}** {status_icon}")
        st.caption(f"📅 {milestone['target_date']}")
    
    with col3:
        # 삭제 버튼
        if st.button("🗑️", key=f"del_milestone_{milestone['id']}"):
            db.delete_milestone(milestone['id'])
            st.rerun()
```

---

## 7. F-006: 회고 기록 (KPT)

### 7.1 회고 작성

**UI**:
```python
st.markdown("## 📝 프로젝트 회고 (KPT)")

# 프로젝트 정보
project = db.get_project(project_id)
st.info(f"📅 {project['start_date']} ~ {project['target_end_date']}")

# 기존 회고 불러오기
retro = db.get_retrospective(project_id)

with st.form("retrospective"):
    st.markdown("### 🟢 Keep (계속할 것)")
    keep = st.text_area(
        "잘했던 것, 계속 유지할 것",
        value=retro['keep_content'] if retro else "",
        height=100
    )
    
    st.markdown("### 🔴 Problem (문제점)")
    problem = st.text_area(
        "개선이 필요한 것",
        value=retro['problem_content'] if retro else "",
        height=100
    )
    
    st.markdown("### 🔵 Try (시도할 것)")
    try_content = st.text_area(
        "다음에 시도해볼 것",
        value=retro['try_content'] if retro else "",
        height=100
    )
    
    st.markdown("### 💡 학습 내용")
    learning = st.text_area(
        "배운 것, 기술/개념",
        value=retro['learning_content'] if retro else "",
        height=100
    )
    
    col1, col2 = st.columns(2)
    with col1:
        save = st.form_submit_button("저장", type="primary")
    with col2:
        clear = st.form_submit_button("초기화")
    
    if save:
        data = {
            'keep_content': keep,
            'problem_content': problem,
            'try_content': try_content,
            'learning_content': learning
        }
        
        if retro:
            db.update_retrospective(project_id, data)
        else:
            db.insert_retrospective(project_id, data)
        
        st.success("회고가 저장되었습니다!")
    
    if clear:
        st.rerun()
```

### 7.2 회고 조회

**과거 회고 목록**:
```python
st.markdown("### 📚 과거 회고")

retros = db.get_all_retrospectives()

for retro in retros:
    with st.expander(f"{retro['project_name']} ({retro['created_at']})"):
        st.markdown(f"**Keep**: {retro['keep_content']}")
        st.markdown(f"**Problem**: {retro['problem_content']}")
        st.markdown(f"**Try**: {retro['try_content']}")
        st.markdown(f"**학습**: {retro['learning_content']}")
```

---

## 8. 데이터베이스 함수 명세

### 8.1 프로젝트 관련

```python
def insert_project(project_data: dict) -> int:
    """프로젝트 생성, ID 반환"""

def get_project(project_id: int) -> dict:
    """프로젝트 조회"""

def get_projects(status: str = None) -> list:
    """프로젝트 목록 조회"""

def update_project(project_id: int, updates: dict) -> bool:
    """프로젝트 수정"""

def delete_project(project_id: int) -> bool:
    """프로젝트 삭제 (CASCADE)"""
```

### 8.2 태스크 관련

```python
def insert_task(task_data: dict) -> int:
    """태스크 생성"""

def get_task(task_id: int) -> dict:
    """태스크 조회"""

def get_tasks(project_id: int, status: str = None) -> list:
    """프로젝트의 태스크 목록"""

def update_task(task_id: int, updates: dict) -> bool:
    """태스크 수정"""

def update_task_status(task_id: int, new_status: str) -> bool:
    """태스크 상태 변경 (시간 기록 포함)"""

def delete_task(task_id: int) -> bool:
    """태스크 삭제"""

def count_tasks(project_id: int) -> int:
    """태스크 개수"""
```

### 8.3 마일스톤 관련

```python
def insert_milestone(project_id: int, title: str, 
                     description: str, target_date: date) -> int:
    """마일스톤 생성"""

def get_milestones(project_id: int) -> list:
    """마일스톤 목록"""

def update_milestone_status(milestone_id: int, 
                            is_completed: bool) -> bool:
    """마일스톤 완료 상태 변경"""

def delete_milestone(milestone_id: int) -> bool:
    """마일스톤 삭제"""
```

### 8.4 회고 관련

```python
def insert_retrospective(project_id: int, data: dict) -> int:
    """회고 생성"""

def get_retrospective(project_id: int) -> dict:
    """특정 프로젝트 회고 조회"""

def get_all_retrospectives() -> list:
    """전체 회고 목록"""

def update_retrospective(project_id: int, data: dict) -> bool:
    """회고 수정"""
```

---

## 9. 에러 처리

### 9.1 DB 연결 오류

```python
def get_db_connection():
    try:
        conn = mysql.connector.connect(**DB_CONFIG)
        return conn
    except mysql.connector.Error as e:
        st.error("❌ 데이터베이스 연결 실패")
        st.error(f"상세: {e}")
        return None
```

### 9.2 입력 검증 오류

```python
def validate_project_input(name, start_date, target_date):
    errors = []
    
    if not name:
        errors.append("프로젝트명은 필수입니다")
    elif len(name) > 200:
        errors.append("프로젝트명은 200자 이내여야 합니다")
    
    if target_date and start_date > target_date:
        errors.append("시작일은 목표일보다 이전이어야 합니다")
    
    return errors

# 사용
errors = validate_project_input(...)
if errors:
    for error in errors:
        st.error(error)
else:
    # 처리
```

### 9.3 데이터 없음

```python
tasks = db.get_tasks(project_id)

if not tasks:
    st.info("📝 아직 태스크가 없습니다. 첫 태스크를 추가해보세요!")
    if st.button("➕ 태스크 추가"):
        # 추가 폼 표시
else:
    # 태스크 목록 표시
```

---

## 10. 테스트 계획

### 10.1 단위 테스트

```python
# test_utils.py
def test_calculate_progress_rate():
    assert calculate_progress_rate(10, 5) == 50.0
    assert calculate_progress_rate(0, 0) == 0.0

def test_days_until():
    future = datetime.today() + timedelta(days=5)
    assert days_until(future) == 5
```

### 10.2 통합 테스트

| 테스트 | 절차 | 예상 결과 |
|--------|------|-----------|
| 프로젝트 생성 | 정보 입력 후 생성 버튼 | DB 저장, 목록에 표시 |
| 태스크 이동 | To Do → In Progress | 상태 변경, started_at 기록 |
| 진행률 계산 | 태스크 5개, 완료 2개 | 40% 표시 |
| 마일스톤 체크 | 체크박스 클릭 | is_completed 업데이트 |

---

**문서 작성일**: 2025년 11월 23일  
**버전**: 1.0
