# 📊 데이터베이스 설계서 - Project Tracker

## 문서 정보
- **프로젝트명**: Project Tracker  
- **DBMS**: MySQL 8.4+
- **문자 인코딩**: utf8mb4
- **버전**: 1.0
- **작성일**: 2025년 11월 23일

---

## 1. 데이터베이스 개요

### 1.1 데이터베이스명
`project_tracker`

### 1.2 설계 원칙
- 제3정규형(3NF) 준수
- 외래키로 참조 무결성 유지
- CASCADE 삭제로 orphan 방지
- 인덱스로 검색 성능 최적화

---

## 2. ER 다이어그램

```
┌──────────────────┐
│    projects      │
│  (프로젝트)      │
├──────────────────┤
│ id (PK)          │
│ name             │
│ description      │
│ start_date       │
│ target_end_date  │
│ status           │
│ created_at       │
│ updated_at       │
└──────────────────┘
         │
         │ 1:N
         ↓
┌──────────────────┐       ┌──────────────────┐
│     tasks        │       │   milestones     │
│  (태스크)        │       │  (마일스톤)      │
├──────────────────┤       ├──────────────────┤
│ id (PK)          │       │ id (PK)          │
│ project_id (FK)  │       │ project_id (FK)  │
│ title            │       │ title            │
│ description      │       │ description      │
│ status           │       │ target_date      │
│ priority         │       │ is_completed     │
│ tags             │       │ completed_at     │
│ estimated_hours  │       │ created_at       │
│ due_date         │       └──────────────────┘
│ started_at       │
│ completed_at     │       ┌──────────────────┐
│ created_at       │       │ retrospectives   │
└──────────────────┘       │   (회고)         │
         │                 ├──────────────────┤
         │ 1:N             │ id (PK)          │
         ↓                 │ project_id (FK)  │
┌──────────────────┐       │ keep_content     │
│ checklist_items  │       │ problem_content  │
│ (체크리스트)     │       │ try_content      │
├──────────────────┤       │ learning_content │
│ id (PK)          │       │ created_at       │
│ task_id (FK)     │       └──────────────────┘
│ content          │
│ is_checked       │
│ created_at       │
└──────────────────┘
```

---

## 3. 테이블 상세 설계

### 3.1 projects (프로젝트)

#### 목적
프로젝트의 기본 정보 저장

#### 테이블 생성 SQL
```sql
CREATE TABLE IF NOT EXISTS projects (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL COMMENT '프로젝트명',
    description TEXT COMMENT '프로젝트 설명',
    start_date DATE NOT NULL COMMENT '시작일',
    target_end_date DATE COMMENT '목표 완료일',
    status ENUM('active', 'completed', 'on_hold') DEFAULT 'active' COMMENT '상태',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '생성일시',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '수정일시',
    
    INDEX idx_status (status),
    INDEX idx_dates (start_date, target_end_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='프로젝트';
```

#### 컬럼 명세

| 컬럼명 | 타입 | NULL | 기본값 | 설명 | 예시 |
|--------|------|------|--------|------|------|
| id | INT | NO | AUTO_INCREMENT | 프로젝트 ID (PK) | 1 |
| name | VARCHAR(200) | NO | - | 프로젝트명 | "감정 일기 앱" |
| description | TEXT | YES | NULL | 설명 | "LLM 기반 감정 분석" |
| start_date | DATE | NO | - | 시작일 | 2024-11-01 |
| target_end_date | DATE | YES | NULL | 목표 완료일 | 2024-11-30 |
| status | ENUM | NO | 'active' | 상태 | 'active' |
| created_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 생성일시 | 2024-11-23 10:00:00 |
| updated_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 수정일시 | 2024-11-23 15:30:00 |

#### status 값
- `active`: 진행 중
- `completed`: 완료
- `on_hold`: 보류

---

### 3.2 tasks (태스크)

#### 목적
프로젝트의 세부 작업 항목 저장

#### 테이블 생성 SQL
```sql
CREATE TABLE IF NOT EXISTS tasks (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_id INT NOT NULL COMMENT '프로젝트 ID (FK)',
    title VARCHAR(200) NOT NULL COMMENT '태스크 제목',
    description TEXT COMMENT '상세 설명',
    status ENUM('todo', 'in_progress', 'done') DEFAULT 'todo' COMMENT '상태',
    priority ENUM('low', 'medium', 'high') DEFAULT 'medium' COMMENT '우선순위',
    tags VARCHAR(200) COMMENT '태그 (쉼표 구분)',
    estimated_hours DECIMAL(5,2) COMMENT '예상 소요 시간 (시간)',
    due_date DATE COMMENT '마감일',
    started_at TIMESTAMP NULL COMMENT '시작 시간',
    completed_at TIMESTAMP NULL COMMENT '완료 시간',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '생성일시',
    
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
    INDEX idx_project (project_id),
    INDEX idx_status (status),
    INDEX idx_priority (priority),
    INDEX idx_due_date (due_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='태스크';
```

#### 컬럼 명세

| 컬럼명 | 타입 | NULL | 기본값 | 설명 | 예시 |
|--------|------|------|--------|------|------|
| id | INT | NO | AUTO_INCREMENT | 태스크 ID (PK) | 1 |
| project_id | INT | NO | - | 프로젝트 ID (FK) | 1 |
| title | VARCHAR(200) | NO | - | 제목 | "LLM API 연동" |
| description | TEXT | YES | NULL | 설명 | "OpenAI API 사용" |
| status | ENUM | NO | 'todo' | 상태 | 'in_progress' |
| priority | ENUM | NO | 'medium' | 우선순위 | 'high' |
| tags | VARCHAR(200) | YES | NULL | 태그 | "Dev,API" |
| estimated_hours | DECIMAL(5,2) | YES | NULL | 예상 시간 | 4.50 |
| due_date | DATE | YES | NULL | 마감일 | 2024-11-25 |
| started_at | TIMESTAMP | YES | NULL | 시작 시간 | 2024-11-21 09:00:00 |
| completed_at | TIMESTAMP | YES | NULL | 완료 시간 | 2024-11-23 16:30:00 |
| created_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 생성일시 | 2024-11-20 10:00:00 |

#### status 값
- `todo`: 할 일
- `in_progress`: 진행 중
- `done`: 완료

#### priority 값
- `low`: 낮음
- `medium`: 보통
- `high`: 높음

---

### 3.3 checklist_items (체크리스트 항목)

#### 목적
태스크의 세부 체크리스트 관리

#### 테이블 생성 SQL
```sql
CREATE TABLE IF NOT EXISTS checklist_items (
    id INT PRIMARY KEY AUTO_INCREMENT,
    task_id INT NOT NULL COMMENT '태스크 ID (FK)',
    content VARCHAR(300) NOT NULL COMMENT '항목 내용',
    is_checked BOOLEAN DEFAULT FALSE COMMENT '체크 여부',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '생성일시',
    
    FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE,
    INDEX idx_task (task_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='체크리스트';
```

#### 컬럼 명세

| 컬럼명 | 타입 | NULL | 기본값 | 설명 | 예시 |
|--------|------|------|--------|------|------|
| id | INT | NO | AUTO_INCREMENT | 항목 ID (PK) | 1 |
| task_id | INT | NO | - | 태스크 ID (FK) | 1 |
| content | VARCHAR(300) | NO | - | 내용 | "API 키 발급" |
| is_checked | BOOLEAN | NO | FALSE | 체크 여부 | TRUE |
| created_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 생성일시 | 2024-11-20 10:00:00 |

---

### 3.4 milestones (마일스톤)

#### 목적
프로젝트의 주요 체크포인트 관리

#### 테이블 생성 SQL
```sql
CREATE TABLE IF NOT EXISTS milestones (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_id INT NOT NULL COMMENT '프로젝트 ID (FK)',
    title VARCHAR(200) NOT NULL COMMENT '마일스톤명',
    description TEXT COMMENT '설명',
    target_date DATE NOT NULL COMMENT '목표 날짜',
    is_completed BOOLEAN DEFAULT FALSE COMMENT '완료 여부',
    completed_at TIMESTAMP NULL COMMENT '완료 시간',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '생성일시',
    
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
    INDEX idx_project (project_id),
    INDEX idx_target_date (target_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='마일스톤';
```

#### 컬럼 명세

| 컬럼명 | 타입 | NULL | 기본값 | 설명 | 예시 |
|--------|------|------|--------|------|------|
| id | INT | NO | AUTO_INCREMENT | 마일스톤 ID (PK) | 1 |
| project_id | INT | NO | - | 프로젝트 ID (FK) | 1 |
| title | VARCHAR(200) | NO | - | 제목 | "MVP 완성" |
| description | TEXT | YES | NULL | 설명 | "핵심 기능 구현 완료" |
| target_date | DATE | NO | - | 목표 날짜 | 2024-11-20 |
| is_completed | BOOLEAN | NO | FALSE | 완료 여부 | TRUE |
| completed_at | TIMESTAMP | YES | NULL | 완료 시간 | 2024-11-20 18:00:00 |
| created_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 생성일시 | 2024-11-01 10:00:00 |

---

### 3.5 retrospectives (회고)

#### 목적
프로젝트 회고 (KPT) 저장

#### 테이블 생성 SQL
```sql
CREATE TABLE IF NOT EXISTS retrospectives (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_id INT NOT NULL COMMENT '프로젝트 ID (FK)',
    keep_content TEXT COMMENT 'Keep - 계속할 것',
    problem_content TEXT COMMENT 'Problem - 문제점',
    try_content TEXT COMMENT 'Try - 시도할 것',
    learning_content TEXT COMMENT '학습 내용',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '작성일시',
    
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
    INDEX idx_project (project_id),
    UNIQUE KEY unique_project (project_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='회고';
```

#### 컬럼 명세

| 컬럼명 | 타입 | NULL | 기본값 | 설명 | 예시 |
|--------|------|------|--------|------|------|
| id | INT | NO | AUTO_INCREMENT | 회고 ID (PK) | 1 |
| project_id | INT | NO | - | 프로젝트 ID (FK) | 1 |
| keep_content | TEXT | YES | NULL | 계속할 것 | "매일 스탠드업" |
| problem_content | TEXT | YES | NULL | 문제점 | "일정 예측 부정확" |
| try_content | TEXT | YES | NULL | 시도할 것 | "버퍼 20% 추가" |
| learning_content | TEXT | YES | NULL | 학습 내용 | "MVVM 패턴" |
| created_at | TIMESTAMP | NO | CURRENT_TIMESTAMP | 작성일시 | 2024-11-30 20:00:00 |

**특징**:
- 프로젝트당 하나의 회고만 저장 (UNIQUE KEY)
- 수정 시 UPDATE 사용

---

## 4. 관계 설계

### 4.1 projects ↔ tasks (1:N)

**관계**: 하나의 프로젝트는 여러 태스크를 가질 수 있음

**외래키**:
```sql
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
```

**CASCADE 규칙**:
- 프로젝트 삭제 → 모든 태스크 자동 삭제

---

### 4.2 tasks ↔ checklist_items (1:N)

**관계**: 하나의 태스크는 여러 체크리스트 항목을 가질 수 있음

**외래키**:
```sql
FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
```

**CASCADE 규칙**:
- 태스크 삭제 → 모든 체크리스트 항목 자동 삭제

---

### 4.3 projects ↔ milestones (1:N)

**관계**: 하나의 프로젝트는 여러 마일스톤을 가질 수 있음

**외래키**:
```sql
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
```

---

### 4.4 projects ↔ retrospectives (1:1)

**관계**: 하나의 프로젝트는 하나의 회고를 가질 수 있음

**외래키**:
```sql
FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
UNIQUE KEY unique_project (project_id)
```

**특징**:
- UNIQUE 제약으로 1:1 관계 보장

---

## 5. 인덱스 전략

### 5.1 설정된 인덱스

```sql
-- projects
INDEX idx_status (status)                      -- 상태별 조회
INDEX idx_dates (start_date, target_end_date)  -- 날짜 범위 검색

-- tasks
INDEX idx_project (project_id)      -- 프로젝트별 태스크 조회
INDEX idx_status (status)            -- 상태별 필터링
INDEX idx_priority (priority)        -- 우선순위별 정렬
INDEX idx_due_date (due_date)        -- 마감일 검색

-- checklist_items
INDEX idx_task (task_id)             -- 태스크별 항목 조회

-- milestones
INDEX idx_project (project_id)       -- 프로젝트별 마일스톤
INDEX idx_target_date (target_date)  -- 날짜 검색

-- retrospectives
INDEX idx_project (project_id)       -- 프로젝트별 회고
```

### 5.2 쿼리 최적화 고려사항

**자주 사용하는 쿼리 패턴**:
1. 프로젝트별 태스크 조회
2. 상태별 태스크 필터링
3. 마감일 기준 정렬
4. 진행률 계산

**복합 인덱스 고려** (성능 개선 필요시):
```sql
-- 프로젝트 + 상태 조합 검색
CREATE INDEX idx_project_status ON tasks(project_id, status);

-- 프로젝트 + 우선순위 조합 검색
CREATE INDEX idx_project_priority ON tasks(project_id, priority);
```

---

## 6. 샘플 데이터

### 6.1 프로젝트 샘플

```sql
INSERT INTO projects (name, description, start_date, target_end_date, status) VALUES
('감정 일기 앱', 'LLM 기반 감정 분석 일기 앱', '2024-11-01', '2024-11-30', 'active'),
('포트폴리오 웹사이트', '개인 포트폴리오 제작', '2024-11-15', '2024-12-15', 'active'),
('알고리즘 스터디', 'LeetCode 100문제 풀기', '2024-11-01', NULL, 'active');
```

### 6.2 태스크 샘플

```sql
-- 감정 일기 앱 (project_id=1) 태스크
INSERT INTO tasks (project_id, title, description, status, priority, tags, due_date) VALUES
(1, 'UI 목업 디자인', 'Figma로 화면 설계', 'done', 'high', 'Design', '2024-11-05'),
(1, 'DB 스키마 설계', 'MySQL 테이블 설계', 'done', 'high', 'Dev', '2024-11-07'),
(1, 'SwiftUI 기본 화면 구현', '일기 작성 화면', 'done', 'high', 'Dev', '2024-11-10'),
(1, 'LLM API 연동', 'OpenAI API 사용', 'in_progress', 'high', 'Dev,API', '2024-11-25'),
(1, '감정 분석 로직 구현', '감정 추출 및 저장', 'in_progress', 'high', 'Dev', '2024-11-26'),
(1, '테스트 및 버그 수정', '전체 기능 테스트', 'todo', 'medium', 'Test', '2024-11-28'),
(1, '발표 자료 준비', 'PPT 작성', 'todo', 'medium', 'Docs', '2024-11-29');
```

### 6.3 체크리스트 샘플

```sql
-- "LLM API 연동" 태스크 (task_id=4)의 체크리스트
INSERT INTO checklist_items (task_id, content, is_checked) VALUES
(4, 'OpenAI API 키 발급', TRUE),
(4, 'API 호출 테스트', TRUE),
(4, '에러 처리 구현', FALSE),
(4, '비용 최적화', FALSE);
```

### 6.4 마일스톤 샘플

```sql
-- 감정 일기 앱 (project_id=1) 마일스톤
INSERT INTO milestones (project_id, title, description, target_date, is_completed, completed_at) VALUES
(1, '기획 완료', 'UI/UX 디자인 및 기능 정의', '2024-11-05', TRUE, '2024-11-05 18:00:00'),
(1, 'MVP 개발 완료', '핵심 기능 구현', '2024-11-20', TRUE, '2024-11-20 22:00:00'),
(1, '베타 테스트', '사용자 테스트 및 피드백', '2024-11-27', FALSE, NULL),
(1, '최종 발표', '프로젝트 발표', '2024-12-01', FALSE, NULL);
```

---

## 7. 쿼리 예제

### 7.1 기본 조회

```sql
-- 모든 활성 프로젝트
SELECT * FROM projects WHERE status = 'active';

-- 특정 프로젝트의 모든 태스크
SELECT * FROM tasks 
WHERE project_id = 1 
ORDER BY FIELD(status, 'in_progress', 'todo', 'done'), priority DESC;

-- 완료된 태스크만
SELECT * FROM tasks 
WHERE project_id = 1 AND status = 'done' 
ORDER BY completed_at DESC;
```

### 7.2 조인 쿼리

```sql
-- 프로젝트와 태스크 함께 조회
SELECT 
    p.name AS project_name,
    t.title AS task_title,
    t.status,
    t.priority
FROM projects p
LEFT JOIN tasks t ON p.id = t.project_id
ORDER BY p.name, t.created_at;

-- 태스크와 체크리스트 함께 조회
SELECT 
    t.title AS task_title,
    c.content AS checklist_item,
    c.is_checked
FROM tasks t
LEFT JOIN checklist_items c ON t.id = c.task_id
WHERE t.project_id = 1;
```

### 7.3 집계 쿼리

```sql
-- 프로젝트별 태스크 통계
SELECT 
    p.name,
    COUNT(t.id) AS total_tasks,
    SUM(CASE WHEN t.status = 'done' THEN 1 ELSE 0 END) AS completed_tasks,
    ROUND(SUM(CASE WHEN t.status = 'done' THEN 1 ELSE 0 END) * 100.0 / COUNT(t.id), 1) AS progress_rate
FROM projects p
LEFT JOIN tasks t ON p.id = t.project_id
GROUP BY p.id, p.name;

-- 우선순위별 태스크 개수
SELECT 
    priority,
    COUNT(*) AS count
FROM tasks
WHERE project_id = 1
GROUP BY priority
ORDER BY FIELD(priority, 'high', 'medium', 'low');

-- 상태별 태스크 개수
SELECT 
    status,
    COUNT(*) AS count
FROM tasks
WHERE project_id = 1
GROUP BY status;
```

### 7.4 날짜 관련 쿼리

```sql
-- 이번 주에 마감인 태스크
SELECT * FROM tasks
WHERE due_date BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 7 DAY)
ORDER BY due_date;

-- 완료된 태스크의 날짜별 집계
SELECT 
    DATE(completed_at) AS date,
    COUNT(*) AS completed_count
FROM tasks
WHERE project_id = 1 AND status = 'done'
GROUP BY DATE(completed_at)
ORDER BY date;
```

---

## 8. 성능 최적화

### 8.1 쿼리 최적화 팁

```sql
-- EXPLAIN으로 쿼리 분석
EXPLAIN SELECT * FROM tasks WHERE project_id = 1 AND status = 'todo';

-- 인덱스 사용 확인
SHOW INDEX FROM tasks;
```

### 8.2 자주 사용하는 뷰 생성 (선택)

```sql
CREATE VIEW project_summary AS
SELECT 
    p.id,
    p.name,
    p.status,
    COUNT(t.id) AS total_tasks,
    SUM(CASE WHEN t.status = 'done' THEN 1 ELSE 0 END) AS completed_tasks,
    ROUND(SUM(CASE WHEN t.status = 'done' THEN 1 ELSE 0 END) * 100.0 / NULLIF(COUNT(t.id), 0), 1) AS progress_rate
FROM projects p
LEFT JOIN tasks t ON p.id = t.project_id
GROUP BY p.id, p.name, p.status;

-- 사용
SELECT * FROM project_summary WHERE status = 'active';
```

---

## 9. 백업 및 복구

### 9.1 백업

```bash
# 전체 데이터베이스 백업
mysqldump -u root -p project_tracker > backup_$(date +%Y%m%d).sql

# 특정 테이블만 백업
mysqldump -u root -p project_tracker projects tasks > backup_core.sql
```

### 9.2 복구

```bash
# 백업 파일에서 복구
mysql -u root -p project_tracker < backup_20241123.sql
```

---

## 10. 마이그레이션 (향후 확장)

### Phase 2 추가 테이블

```sql
-- 사용자 테이블 (다중 사용자 지원)
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 프로젝트 멤버 (팀 협업)
CREATE TABLE project_members (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_id INT NOT NULL,
    user_id INT NOT NULL,
    role ENUM('owner', 'member') DEFAULT 'member',
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE KEY unique_member (project_id, user_id)
);
```

---

## 11. 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0 | 2025-11-23 | 초안 작성 | 구현모 |

---

**문서 작성일**: 2025년 11월 23일  
**버전**: 1.0
